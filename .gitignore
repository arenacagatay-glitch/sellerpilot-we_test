node_modules/
dist/
.DS_Store
# Local agent settings stay untracked, but .claude/bin holds the launcher that
# .mcp.json points at, so it has to be committed.
.claude/*
!.claude/bin/
*.local
.env
.env.*
